{{! --
Это многострочный комментарий
--}}

host := 'localhost'
port := 0b11001000
value := 0b11001000

server = table([
    host - $host$,
    port - $port$,
    debug - $host$
])

kontent := 0b10100111001

main_server = table([
    host - $host$,
    kontent - $kontent$,
    debug - $host$
])